# Choose at least ONE mapping/geocoding + ONE POI provider
OPENCAGE_API_KEY= 76d1b437878e47fc96c1904a82a42e15          # geocoding (easy/free-tier)
GOOGLE_MAPS_API_KEY=your_google_maps_key    # optional: Places/Geocoding/Distance

# POIs / activities (pick one)
OPENTRIPMAP_API_KEY=your_opentripmap_key    # great free POI dataset

# Hotels (two easy approaches)
SERPAPI_API_KEY=your_serpapi_key            # to search Booking/Google results (parsed SERPs)
# (or use Google Places 'lodging' as a proxy for hotels)

# Images (optional, but nice)
UNSPLASH_ACCESS_KEY=your_unsplash_key

# Optional LLM (only if enabling LLM planner)
OPENAI_API_KEY=your_openai_key
